
  {
  "name": "pingy-chatapp",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd Backend && npm start",
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd client && npm run dev\"",
    "install-deps": "cd client && npm install && cd ../Backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },

  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
